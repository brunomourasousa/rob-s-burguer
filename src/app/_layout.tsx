import { Stack, } from "expo-router";

// npm i lucide-react-native

export default function StackLayout () {
    return (
        <Stack
            screenOptions={{
                headerShown: false
            }}
           />
           
    )
}